# Optional: expose ennui-agents-core via Ingress. Adjust host and ingress class for your cluster.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ennui-agents-core
  labels:
    app: ennui-agents-core
spec:
  ingressClassName: nginx
  rules:
    - host: agents.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ennui-agents-core
                port:
                  number: 80
  # tls:
  #   - hosts: [agents.example.com]
  #     secretName: ennui-agents-core-tls
